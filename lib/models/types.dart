enum Units {
  metric('Metric'),
  imperial('Imperial');

  const Units(this.value);
  final String value;
}

enum Gender {
  male('Male'),
  female('Female');

  const Gender(this.value);
  final String value;
}
